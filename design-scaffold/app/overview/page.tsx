import PagePlaceholder from "../../components/ui/PagePlaceholder";

// Homepage / overview — placeholder for now.
export default function OverviewPage() {
  return (
    <PagePlaceholder
      title="Overview"
      subtitle="Your AB Agent homepage. Replace these blocks with real widgets later."
      blocks={3}
    />
  );
}
